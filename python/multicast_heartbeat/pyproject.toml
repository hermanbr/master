[project]
name = "multicast-heartbeat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hermanbr", email = "89782208+hermanbr@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "multicast>=2.1.1",
]

[project.scripts]
multicast-heartbeat = "multicast_heartbeat:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = []
